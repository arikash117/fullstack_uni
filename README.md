# Fullstack Uni Project

## Setup backend
```bash
cd backend
python -m venv .env
.env\Scripts\activate
pip install -r requirements.txt
uvicorn src.main:app --reload
```

## Setup frontend
```bash
cd frontend
npm install
npm run dev
```
